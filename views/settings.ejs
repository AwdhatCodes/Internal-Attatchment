<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Settings</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500&display=swap">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
            background: url('https://cdn.pixabay.com/photo/2022/06/24/13/57/woman-7281733_960_720.jpg') no-repeat center center fixed;
            background-size: cover;
            display: flex;
            min-height: 100vh;
            overflow: hidden;
        }

        .menu-icon {
            font-size: 1.5em; /* Smaller font size */
            cursor: pointer;
            position: fixed;
            top: 15px;
            left: 15px;
            color: #d3d3d3; /* Light grey color */
            z-index: 1000;
        }

        .sidebar {
            background-color: rgba(30, 30, 30, 0.9); /* Darker translucent */
            width: 250px;
            height: 100vh;
            box-shadow: 4px 0 8px rgba(0, 0, 0, 0.6);
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 60px;
            position: fixed;
            top: 0;
            left: 0;
            transform: translateX(-100%);
            transition: transform 0.3s ease-in-out;
        }

        .sidebar.active {
            transform: translateX(0);
        }

        .sidebar-title {
            color: #f0f0f0;
            font-size: 1.5em;
            margin-bottom: 30px;
            text-align: center;
        }

        .sidebar ul {
            list-style-type: none;
            padding: 0;
            width: 100%;
        }

        .sidebar ul li {
            margin-bottom: 15px;
            width: 100%;
        }

        .sidebar ul li a {
            display: flex;
            align-items: center;
            justify-content: flex-start;
            color: #f0f0f0; /* Light grey text color */
            text-align: left;
            padding: 15px 20px;
            text-decoration: none;
            transition: background-color 0.3s, color 0.3s, transform 0.3s;
            border-radius: 8px;
            transform: perspective(1000px) rotateY(0deg);
        }

        .sidebar ul li a:hover {
            background-color: rgba(255, 255, 255, 0.2); /* Slightly lighter on hover */
            color: #ffffff;
            transform: perspective(1000px) rotateY(10deg); /* 3D effect on hover */
        }

        .sidebar ul li a i {
            margin-right: 15px;
            font-size: 1.2em;
        }

        main {
            flex: 1;
            margin-left: 250px;
            padding: 20px;
            background-color: rgba(255, 255, 255, 0.9);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            border-radius: 12px;
            transition: margin-left 0.3s;
        }

        .sidebar.active + main {
            margin-left: 250px;
        }

        header {
            background-color: rgba(15, 15, 15, 0.9); /* Darker translucent background for header */
            color: #fff;
            padding: 12px 0;
            text-align: center;
            border-radius: 12px;
            margin-bottom: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        header h1 {
            margin: 0;
            font-weight: 500;
        }

        main p {
            font-size: 1.1em;
            margin-bottom: 20px;
        }

        .settings-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }

        .settings-section {
            background: rgba(0, 0, 0, 0.7); /* Darker background */
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 12px;
            padding: 20px;
            margin: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            width: 300px;
            transition: transform 0.3s, background 0.3s;
        }

        .settings-section:hover {
            transform: scale(1.05);
            background: rgba(0, 0, 0, 0.8); /* Slightly lighter on hover */
        }

        .settings-section h2 {
            margin-top: 0;
            color: #fff;
            font-size: 1.3em;
            font-weight: 500;
        }

        .circular-input {
            position: relative;
            width: 160px;
            height: 160px;
            margin: 20px auto;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.2em;
            color: #fff;
            box-shadow: inset 0 4px 6px rgba(0, 0, 0, 0.4);
            transition: background 0.3s;
        }

        .circular-input input {
            position: absolute;
            width: 100%;
            height: 100%;
            opacity: 0;
            cursor: pointer;
        }

        .temperature {
            background: conic-gradient(#ff5722 0 calc(var(--value) * 1%), #333 calc(var(--value) * 1%) 100%);
        }

        .humidity {
            background: conic-gradient(#00bcd4 0 calc(var(--value) * 1%), #333 calc(var(--value) * 1%) 100%);
        }

        .light-intensity {
            background: conic-gradient(#ffeb3b 0 calc(var(--value) * 1%), #333 calc(var(--value) * 1%) 100%);
        }
    </style>
</head>
<body>
    <div class="menu-icon" onclick="toggleSidebar()">
        <i class="fas fa-bars" id="menuIcon"></i>
    </div>
    <nav class="sidebar" id="sidebar">
        <div class="sidebar-title">SEED-CORE</div>
        <ul>
            <li><a href="/dashboard"><i class="fas fa-tachometer-alt"></i>Dashboard</a></li>
            <li><a href="/profile"><i class="fas fa-user"></i>Profile</a></li>
            <li><a href="/settings"><i class="fas fa-cog"></i>Settings</a></li>
            <li><a href="/rooms"><i class="fas fa-door-open"></i>Rooms</a></li>
            <li><a href="/logout"><i class="fas fa-sign-out-alt"></i>Logout</a></li>
        </ul>
    </nav>
    <main>
        <header>
            <h1>Settings</h1>
        </header>
        <p>Manage your settings here.</p>
        <div class="settings-container">
            <div class="settings-section">
                <h2>Temperature Settings</h2>
                <div class="circular-input temperature" style="--value: 50;">
                    <span>25°C</span>
                    <input type="range" min="0" max="50" value="25" step="1" oninput="updateInput(this, '°C')">
                </div>
            </div>
            <div class="settings-section">
                <h2>Humidity Settings</h2>
                <div class="circular-input humidity" style="--value: 60;">
                    <span>60%</span>
                    <input type="range" min="0" max="100" value="60" step="1" oninput="updateInput(this, '%')">
                </div>
            </div>
            <div class="settings-section">
                <h2>Light Intensity Settings</h2>
                <div class="circular-input light-intensity" style="--value: 70;">
                    <span>70%</span>
                    <input type="range" min="0" max="100" value="70" step="1" oninput="updateInput(this, '%')">
                </div>
            </div>
        </div>
    </main>
    <script>
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            const menuIcon = document.getElementById('menuIcon');
            sidebar.classList.toggle('active');
            if (sidebar.classList.contains('active')) {
                menuIcon.classList.remove('fa-bars');
                menuIcon.classList.add('fa-times');
            } else {
                menuIcon.classList.remove('fa-times');
                menuIcon.classList.add('fa-bars');
            }
        }

        function updateInput(element, unit) {
            const value = element.value;
            element.parentElement.style.setProperty('--value', value);
            element.previousElementSibling.innerText = `${value}${unit}`;
        }
    </script>
</body>
</html>
